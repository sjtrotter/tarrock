class_name WorldStateIds
extends RefCounted

## Every world-state flag id, as a constant.
##
## GENERATED by `godot/tools/gen_definitions.py` from
## `docs/design/world.md §World-state matrix` - do not edit by hand; edit the doc and
## regenerate. A drift test fails when this file and the matrix disagree.
##
## Code never types a `WS_*` string: it names one of these constants, or reads
## an id off a `WorldStateDefinition` (docs/design/technical.md, no magic
## strings). The `.tres` data under `res://data/world_states/` is generated
## from the same rows, so the two cannot drift apart.

## The unbinding flags, in card order (MQ01 first).
const WS_MAGICIAN_UNBOUND := &"WS_MAGICIAN_UNBOUND"  # MQ01
const WS_PRIESTESS_UNBOUND := &"WS_PRIESTESS_UNBOUND"  # MQ02
const WS_EMPRESS_UNBOUND := &"WS_EMPRESS_UNBOUND"  # MQ03
const WS_EMPEROR_UNBOUND := &"WS_EMPEROR_UNBOUND"  # MQ04
const WS_HIEROPHANT_UNBOUND := &"WS_HIEROPHANT_UNBOUND"  # MQ05
const WS_LOVERS_UNBOUND := &"WS_LOVERS_UNBOUND"  # MQ06
const WS_CHARIOT_UNBOUND := &"WS_CHARIOT_UNBOUND"  # MQ07
const WS_STRENGTH_UNBOUND := &"WS_STRENGTH_UNBOUND"  # MQ08
const WS_HERMIT_UNBOUND := &"WS_HERMIT_UNBOUND"  # MQ09
const WS_FORTUNE_UNBOUND := &"WS_FORTUNE_UNBOUND"  # MQ10
const WS_JUSTICE_UNBOUND := &"WS_JUSTICE_UNBOUND"  # MQ11
const WS_HANGEDMAN_UNBOUND := &"WS_HANGEDMAN_UNBOUND"  # MQ12
const WS_DEATH_UNBOUND := &"WS_DEATH_UNBOUND"  # MQ13
const WS_TEMPERANCE_UNBOUND := &"WS_TEMPERANCE_UNBOUND"  # MQ14
const WS_DEVIL_UNBOUND := &"WS_DEVIL_UNBOUND"  # MQ15
const WS_TOWER_UNBOUND := &"WS_TOWER_UNBOUND"  # MQ16
const WS_STAR_UNBOUND := &"WS_STAR_UNBOUND"  # MQ17
const WS_MOON_UNBOUND := &"WS_MOON_UNBOUND"  # MQ18
const WS_SUN_UNBOUND := &"WS_SUN_UNBOUND"  # MQ19
const WS_JUDGEMENT_UNBOUND := &"WS_JUDGEMENT_UNBOUND"  # MQ20
const WS_WORLD_UNBOUND := &"WS_WORLD_UNBOUND"  # MQ21

## The branch flags a quest's closing choice picks between.
const WS_TROUPE_TRAVELING := &"WS_TROUPE_TRAVELING"  # MQ01, MQ01_TROUPE
const WS_TROUPE_SETTLED := &"WS_TROUPE_SETTLED"  # MQ01, MQ01_TROUPE
const WS_DIVIDE_EASTMARRIED := &"WS_DIVIDE_EASTMARRIED"  # MQ06, MQ06_DIVIDE
const WS_DIVIDE_WESTMARRIED := &"WS_DIVIDE_WESTMARRIED"  # MQ06, MQ06_DIVIDE

## Every unbinding flag, in card order.
const UNBINDING: Array[StringName] = [
	WS_MAGICIAN_UNBOUND,
	WS_PRIESTESS_UNBOUND,
	WS_EMPRESS_UNBOUND,
	WS_EMPEROR_UNBOUND,
	WS_HIEROPHANT_UNBOUND,
	WS_LOVERS_UNBOUND,
	WS_CHARIOT_UNBOUND,
	WS_STRENGTH_UNBOUND,
	WS_HERMIT_UNBOUND,
	WS_FORTUNE_UNBOUND,
	WS_JUSTICE_UNBOUND,
	WS_HANGEDMAN_UNBOUND,
	WS_DEATH_UNBOUND,
	WS_TEMPERANCE_UNBOUND,
	WS_DEVIL_UNBOUND,
	WS_TOWER_UNBOUND,
	WS_STAR_UNBOUND,
	WS_MOON_UNBOUND,
	WS_SUN_UNBOUND,
	WS_JUDGEMENT_UNBOUND,
	WS_WORLD_UNBOUND,
]

## Every branch flag, in matrix order.
const BRANCH: Array[StringName] = [
	WS_TROUPE_TRAVELING,
	WS_TROUPE_SETTLED,
	WS_DIVIDE_EASTMARRIED,
	WS_DIVIDE_WESTMARRIED,
]

## Every flag the matrix defines.
const ALL: Array[StringName] = [
	WS_MAGICIAN_UNBOUND,
	WS_TROUPE_TRAVELING,
	WS_TROUPE_SETTLED,
	WS_PRIESTESS_UNBOUND,
	WS_EMPRESS_UNBOUND,
	WS_EMPEROR_UNBOUND,
	WS_HIEROPHANT_UNBOUND,
	WS_LOVERS_UNBOUND,
	WS_DIVIDE_EASTMARRIED,
	WS_DIVIDE_WESTMARRIED,
	WS_CHARIOT_UNBOUND,
	WS_STRENGTH_UNBOUND,
	WS_HERMIT_UNBOUND,
	WS_FORTUNE_UNBOUND,
	WS_JUSTICE_UNBOUND,
	WS_HANGEDMAN_UNBOUND,
	WS_DEATH_UNBOUND,
	WS_TEMPERANCE_UNBOUND,
	WS_DEVIL_UNBOUND,
	WS_TOWER_UNBOUND,
	WS_STAR_UNBOUND,
	WS_MOON_UNBOUND,
	WS_SUN_UNBOUND,
	WS_JUDGEMENT_UNBOUND,
	WS_WORLD_UNBOUND,
]
